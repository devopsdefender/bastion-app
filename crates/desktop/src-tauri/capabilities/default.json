{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Desktop capabilities — no fs, no shell, no process. All heavy lifting (PTY, transport, crypto) lives in Rust commands; the webview only calls invoke().",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
